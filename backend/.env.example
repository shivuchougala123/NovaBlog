# MongoDB Connection
MONGO_URI=mongodb://mongodb:27017/novablog

# JWT Secret (CHANGE THIS IN PRODUCTION!)
JWT_SECRET=your-super-secret-jwt-key-change-me-in-production

# Server Port
PORT=5000

# Notes:
# - For Docker deployment, environment variables are set in docker-compose.yml
# - For local development, copy this file to .env and update the values
# - Change MONGO_URI to your MongoDB connection string if using MongoDB Atlas
